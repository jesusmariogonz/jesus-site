/* Pega esto al final de app/globals.css (solo si aun no lo tienes de la actualizacion anterior) */

/* ── Producto promocional / gratis ──────────────────────────── */
.tk-card-badge {
  position: absolute;
  top: 16px;
  left: 16px;
  background: #ffffff;
  color: #0a0a0a;
  font-family: var(--font-mono);
  font-weight: 700;
  font-size: 0.68rem;
  letter-spacing: 0.05em;
  text-transform: uppercase;
  padding: 7px 14px;
  border-radius: 9999px;
  box-shadow: 0 10px 26px -6px rgba(255, 255, 255, 0.35);
  z-index: 2;
}
.tk-card-gratis {
  color: #ffffff;
  font-weight: 700;
}
